{
  "name": "sgc-premonition-backend",
  "version": "1.0.0",
  "description": "SGC Premonition — PR Intelligence / AI Foresight / Brand Protection. v1 backend: signup, Stripe $9.99/mo subscription, Premonition Matrix scoring engine.",
  "main": "server.js",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "migrate": "node db/migrate.js",
    "seed": "node db/seed.js",
    "test": "node --test \"tests/*.test.js\"",
    "smoke": "node --env-file=.env tests/smoke.mjs",
    "stripe:listen": "stripe listen --forward-to localhost:3000/api/webhook/stripe"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.1",
    "stripe": "^17.5.0"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.7.0",
    "resend": "^4.0.1"
  },
  "//optionalDependencies": {
    "better-sqlite3": "Only needed on Node < 22.5. Node 22.5+ has node:sqlite built in and the app uses that automatically.",
    "resend": "Only needed if you want email alerts. The app runs fine without it."
  }
}
